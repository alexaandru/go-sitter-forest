
pragma solidity 0.4;
pragma solidity >=0.5.0 <0.7.0;

library a {}
library b {}
library c {}
contract test {
    function f(uint a, uint b);
    function g(uint c);
}
contract c {
    event e(uint[10] a, bytes7[8] indexed b, c[3] x);
}
contract c {
    function f() {
        uint8[10 * 2] x;
    }
}
