
(
  (comment)* @doc
  .
  (entity_definition
   (name) @name) @definition.class
)

(namespace) @definition.module
(context_definition) @definition.module
(service_definition) @definition.module

(from_path . (identifier) @reference.class)
